// Hanterar routningen

import route from "./routing.js";

//Måste först dra igång routningen genom att skapa händelse-hantering när DOM är laddad
document.addEventListener("DOMContentLoaded", route);
